# 치킨 증바람

증강 칼바람 내전의 대전 일정, 결과, 선수 티어를 기록하는 반응형 웹사이트입니다.

```bash
npm install
npm run dev
npm test
```

관리자는 Sites 런타임 환경 변수 `ADMIN_EMAILS`에 이메일을 쉼표로 구분해 등록합니다. 로그인 사용자의 이메일이 목록에 포함되면 대전 생성 기능이 표시되고, 서버에서도 동일한 권한을 검증합니다.
